@model CatFactNetwise.Models.MessageModel

@{
    //ViewData["Title"] = "Home Page";
}


<input type="submit" value="Generuj do TXT" style="border:3px black solid" onclick="location.href='@Url.Action("GetFact", "Home")'" />
@if (Model != null)
{
    <h3>@Model.Text</h3>
}






